// Deno-side mirror of src/lib/kidsCatalogTaxonomy.ts (v1).
// Re-export the same values for edge-function consumers so the storefront
// filters and the concept/publish pipeline share one vocabulary.
export * from "../../../src/lib/kidsCatalogTaxonomy.ts";
